{
  "name": "xo2-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ xo2-subgraph",
    "create-local": "graph create --node http://localhost:8020/ xo2-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ xo2-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 xo2-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.23.2",
    "@graphprotocol/graph-ts": "0.23.1",
    "@protofire/subgraph-toolkit": "^0.1.2"
  }
}
